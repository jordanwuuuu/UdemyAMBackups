let notes = ['notes 1', 'notes 2', 'notes 3']

//foreach only takes a function
notes.forEach(function(item, index){
    //the first parameter is the ietm in the array
    //the second pararmeter is the index
    //this is run for every item in the array
    console.log(item);
    console.log(index)

    //this is  callback function
    //this is when we pass a function through a function
});




console.log(notes);