let notes = ['notes 1', 'notes 2', 'notes 3']

//This removes te last time from the array
// notes.pop()

// //Adds to an array
// notes.push('My new note')

// //This removes an item from the start
// notes.shift();
// //this adds an item at the start
// notes.unshift('New note 1');

//this adds to the middle of the array
//first parameter tells it where to start removing, 
//second is how many it will remove, 2 would removes note 2 & 3
//third parameter adds in a new item
//notes.splice(1, 1, 'New notes 2');




console.log(notes.length);
console.log(notes);